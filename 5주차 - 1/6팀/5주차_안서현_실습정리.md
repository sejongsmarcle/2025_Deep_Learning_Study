1. 프로젝트 소개
 
  - 목표 : 작물 잎 사진의 종류와 질병 유무를 분류하기
  
  - 데이터 수 : 40,000개

  - 각 폴더는 클래스 명으로 저장되어있으며, 해당 폴더마다 들어있는 사진들은 그 폴더의 클래스에 포함된다.

2. 데이터 분할

<pre>
import os
import shutil
 
# 원본 데이터셋이 위치한 경로를 지정합니다.
original_dataset_dir = './dataset'

# os.llstdirQ 메서드는 해당 경로 하위에 있는 모든 폴더의 목록을 가져오는 메서드입니다. 이 경우 폴더 목록은 클래스의 목록에 해당하므로 이것을 class_l1st로 저장합니다.
classes_list = os.listdir(original_dataset_dir)

# 나눈 데이터를 저장할 폴더를 생성합니다.
base_dir = './splitted'
os.mkdir(base_dir)

# 분리 후에 각 데이터를 저장할 하위 폴더 train, val, test를 생성합니다.
train_dir = os.path.join(base_dir, 'train')
os.mkdir(train_dir)
validation_dir = os.path.join(base_dir, 'val')
os.mkdir(validation_dir)
test_dir = os.path.join(base_dir, 'test')
os.mkdir(test_dir)

# train, validation, test 폴더 하위에 각각 클래스 목록 폴더를 생성합니다.
for clss in classes_list:
    os.mkdir(os.path.join(train_dir, clss))
    os.mkdir(os.path.join(validation_dir, clss))
    os.mkdir(os.path.join(test_dir, clss))

</pre>
